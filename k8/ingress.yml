apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rdicidr
  namespace: production
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: rdicidr
            port:
              number: 8080